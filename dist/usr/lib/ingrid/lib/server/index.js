var colors = require("colors");

exports.start = start;

function start(argv) {
    console.log("In".green + "Grid".cyan, "server starting...");
    console.log("Server could not be started.".red);
    console.log("");
};